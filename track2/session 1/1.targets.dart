/** 
 * ? Class
 * ? Object
 * ? Static variables
 * ? Constructor Method
 * ? Named Constructor
 * ? Encapsulation
 * ? Inheritance
 * ? Abstraction
 * ? Polymorphism
 * ? Interface
 * ? mixin
 * ? generics
 * ? Factories
 * ? seal & final & Enum
*/


/**
 * Domain
 * *  Entity
 * *  Repo
 * *  UseCases
 */

